# 体验测试与裁决 API 配置（复制为 .env 后填写，不要提交 .env）
# 复制本文件：cp .env.example .env

# 裁决 API 地址（必填才能跑体验测试）
# 推荐：先在本机起本地裁决服务 → npm run adjudication-server，然后填：
#   ADJUDICATION_API=http://localhost:3000/intent/resolve
# 或使用微信云函数 URL 化后的地址（见 docs/云开发_裁决API部署指南.md）
ADJUDICATION_API=

# 测试 AI 用的大模型 Key（必填才能跑体验测试，与云函数里用的相同即可）
DEEPSEEK_API_KEY=

# 可选：腾讯混元，若设置则优先于 DeepSeek
# HUNYUAN_API_KEY=

# 可选：本次体验人设，例如女性向
# PLAYTEST_PERSONA=女性玩家，主要体验恋爱、社交等女性向内容
